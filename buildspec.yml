version: 0.2
phases:
  install:
    commands:
      - cp .env.example .env
      - php artisan key:generate
      - echo Istalling composer dependencies
      - composer install --no-progress --no-suggest
  build:
    commands:
      - ./vendor/bin/phpunit
  post_build:
    commands:
      - pwd
